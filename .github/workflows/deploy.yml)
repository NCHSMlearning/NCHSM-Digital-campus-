name: Deploy to GitHub Pages

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Generate config.js from GitHub Secrets
        run: |
          echo "🔧 Generating config.js from GitHub Secrets..."
          
          # Create config.js with proper escaping for JavaScript
          cat > config.js << 'EOF'
          // config.js - Generated from GitHub Secrets
          // DO NOT EDIT - This file is auto-generated
          window.APP_CONFIG = {
              SUPABASE_URL: "${{ secrets.SUPABASE_URL }}",
              SUPABASE_ANON_KEY: "${{ secrets.SUPABASE_ANON_KEY }}",
              LOCATIONIQ_API_KEY: "${{ secrets.LOCATIONIQ_API_KEY || '' }}",
              BUILD_TIME: "${{ github.run_started_at }}",
              COMMIT_SHA: "${{ github.sha }}",
              ENVIRONMENT: "${{ github.ref == 'refs/heads/main' && 'production' || 'development' }}"
          };
          EOF
          
          # Verify the file was created
          echo "✅ Created config.js"
          echo "📁 File exists: $(ls -la config.js)"
          echo "📄 File contents (first 3 lines):"
          head -n 3 config.js
          echo ""
          echo "🔍 Checking for placeholders..."
          if grep -q '%%' config.js; then
            echo "⚠️ Warning: Placeholders found!"
            grep '%%' config.js
          else
            echo "✅ No placeholders found"
          fi
          
          echo "🔑 Config status:"
          echo "   SUPABASE_URL: ${#secrets.SUPABASE_URL} characters"
          echo "   SUPABASE_ANON_KEY: ${#secrets.SUPABASE_ANON_KEY} characters"
          echo "   LOCATIONIQ_API_KEY: ${#secrets.LOCATIONIQ_API_KEY} characters"
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'
          retention-days: 1
  
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
